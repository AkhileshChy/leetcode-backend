import express from 'express';

const router = express.Router();

router.get('/', );
router.post('/problem/:problemId', );
router.post('/problem/create', );
router.post('/problem/delete/:problemId', );
router.put('/problem/update/:problemId', );

export default router;